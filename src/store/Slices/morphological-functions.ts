import { createSlice} from "@reduxjs/toolkit";

const MorphologicalFunctions = createSlice({
    name: "morphological-functions",
    initialState: [],
    reducers: {}
});


export default MorphologicalFunctions;